const nextConfig = {
  output: "export",
  trailingSlash: true,

  // 🔥 สำคัญมากสำหรับ GitHub Pages
  basePath: "/AgriLink",  // 👈 ชื่อ repo
  assetPrefix: "/AgriLink/",
}

module.exports = nextConfig
